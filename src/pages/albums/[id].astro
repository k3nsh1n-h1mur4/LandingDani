---
export const prerender = false
import { Image } from "astro:assets";
import { getCollection } from "astro:content";
import Layout from "@layouts/Layout.astro";

const albums = await getCollection("albums");
console.log(albums);
/*export function getStaticPaths() {
  albums.map((album) => {
    console.log(album.id);
    return  {
        params: {id: album.id},
    }
  });
}*/

/*const pages = albums.map((album) => {
    return  album;
  //console.log(album.id);
  //console.log(album.data.title);
});

pages.map((page) => {
    console.log(page.id);
    return {
        params: {id: page.id},
    }
});
*/
export async function getStaticPaths() {
    const pages = albums.map((album) => {
        return {
            params: { id: album.id },
            props: { id: album.id, title: album.data.title, description: album.data.description, cover: album.data.cover },
        };
    });
    return pages;
}

const { id, title, description, cover } = Astro.props;
---


<div class="container">
    <div class="title">
        <h1>Galeria</h1>
    </div>
    <div class="container-grid">
        {
            
            albums.map((page) => (
                <a href={`/albums/${page.id}`} class="group transition-all mb-4 block">
                    <Image
                        src={page.data.cover}
                        alt={page.data.title}
                        width={720}
                        quality="mid"
                        class:list={[
                            "rounded-lg transition-all",
                            "group-hover:shadow-lg group-hover:opacity-90",
                        ]}
                    />
                </a>
                    <div class="mt-4 text-center text-neutral-700">
                        <p class="text-xs">{page.data.description}</p>
                    </div> 
            ))
        }
    </div>
</div>

<style>
    .container {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        max-width: 1200px;
        margin: 0 auto;
    }
    .title {
        text-align: center;
    }
    .container-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        grid-gap: 10px;
    }
</style>
